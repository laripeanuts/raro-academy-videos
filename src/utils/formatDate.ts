export function formatDate(dataDate: Date) {
  const date = new Date(dataDate);
  const day = date.getDate();
  const month = date.getMonth() + 1;
  const year = date.getFullYear();
  return `${day}/${month}/${year}`;
}
